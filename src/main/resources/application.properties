spring.application.name=appmax

# === Configura��o do banco de dados PostgreSQL ===
spring.datasource.url=jdbc:postgresql://localhost:5432/database
spring.datasource.username=postgres
spring.datasource.password=eld2025
spring.datasource.driverClassName=org.postgresql.Driver

# === Configura��o do JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Porta do servidor (padr�o: 8080) (Mudada para não entrar em conflito com EvolutionAPI) ===
server.port=8081
